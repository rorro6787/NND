import unittest
from neuro_disease_detector.data_processing.extract_dataset import extract_dataset

class TestDataProcessing(unittest.TestCase):
    def test_1(self):
        self.assertTrue(True)
        self.assertFalse(False)
